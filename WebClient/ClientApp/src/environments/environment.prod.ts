// export const environment = {
//   production: true
// };

import { AppConfig } from '../app/app.config'

export const APP_CONFIG: AppConfig = {
  production: true,
  applicationEndpoint: 'http://http://tnja.in/',
  appServiceEndpoint: 'http://demo.tnja.in/',
  appInsightsInstrumentKey: '',
  envType: 'local'
};